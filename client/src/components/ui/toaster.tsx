'use client';

// Minimal Toaster stub — replace with shadcn/ui toast when available
export function Toaster() {
  return <div id="toaster-root" className="fixed bottom-4 right-4 z-50 space-y-2" />;
}
